query USTesting {
  allUsTestings {
    nodes {
      date
      totalTestResults
      negativeIncrease
      positiveIncrease
      negative
      positive
      inconclusive
      inconclusiveIncrease
    }
  }
}

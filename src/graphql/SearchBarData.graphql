query SearchBarData {
  counties: allCountySummaryViews(condition: {}) {
    nodes {
      countyName
      stateName
      countyFipsCode
      confirmedCases
    }
  }
  states: allStatesSummaries {
    nodes {
      stateName
      stateFipsCode
      confirmedCases
    }
  }
}

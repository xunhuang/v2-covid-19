query USTesting {
  allUsTestings {
    nodes {
      date
      totalTestResults
      negativeIncrease
      positiveIncrease
      stateName
      negative
      totalTestResultsIncrease
      positive
      inconclusive
      inconclusiveIncrease
    }
  }
}
